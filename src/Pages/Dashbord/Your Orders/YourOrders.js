import React from "react";
const YourOrders = ({ product, totall }) => {
  const { productName, price, email, name, _id, phoneNumber } = product;

  return <>{/* <button>jdjdjjd</button> */}</>;
};

export default YourOrders;
